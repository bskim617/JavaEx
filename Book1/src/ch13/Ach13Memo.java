package ch13;

public class Ach13Memo {
	/*
	 * 내용 : 내부 클래스, 람다식, 스트림
	 * - 내부 클래스
	 * 인스턴스 내부 클래스, 정적 내부 클래스, 지역 내부 클래스, 익명 내부 클래스
	 * - 람다식
	 * (매개변수) -> {실행문;}
	 * (x, y) -> x >= y ? x : y(조건문)
	 * - 스트림
	 * 여러 자료의 처리에 대한 기능을 구현해 놓은 클래스
	 * 배열 컬랙션 등의 자료를 일관성 있게 처리 가능
	 */
}
